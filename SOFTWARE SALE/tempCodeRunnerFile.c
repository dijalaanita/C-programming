else if (quantityNumber < 10)
                            // {
                            //     oldTotal = price * quantityNumber;
                            //     printf("%s %.1f", "Your total price is ", discountTotal);
                            // }